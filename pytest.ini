[pytest]
addopts = -m "not slow"
testpaths = tests
markers =
    unit: function, class, or module-level tests
    integration: multi-module tests using tiny generated files
    smoke: environment and entrypoint checks
    regression: historical bug reproductions
    sanity: data-shape and project consistency checks
    slow: long-running tests excluded by default
